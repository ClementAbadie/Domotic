

#include "conf/conf.h"

#include "AI/DB.h"
#include "AI/AI.h"

#include "user/pushbullet.hpp"
#include "Test.h"
#include "user/ServerSocket.h"
#include "user/ServerTCP.h"

#include "user/TCP/TidyServer.h"
#include <iostream>


using namespace std;

/*
volatile struct dataStruct {
	pthread_mutex_t* myMutex = ( pthread_mutex_t*) malloc(sizeof( pthread_mutex_t));
	volatile struct DBs* myDBs = (volatile struct DBs*) malloc(sizeof(volatile struct DBs));

};
*/


